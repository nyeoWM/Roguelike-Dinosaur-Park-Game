package game.skills;

/**
 *  Enum to determine the type of floor. A safer way to determine
 *   the nature of the class, ie if a dirt or a grass should
 *   change into a tree
 */
public enum GroundType {
	TREE, DIRT, GRASS, WATER, REED
}
